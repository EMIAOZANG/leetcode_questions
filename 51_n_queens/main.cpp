#include <iostream>
#include <vector>
#include <string>
#include <bitset>

using namespace std;

class Solution {
public:
    bool isValid(int col){
      //using bitset to store the occupancy
    }
    void DFS(vector<vector<string> > board, int line){
      if ()
    }
    vector<vector<string> > solveNQueens(int n) {
       
    }
};

int main(){
  return 0;
}
